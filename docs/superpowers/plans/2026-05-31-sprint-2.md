# Sprint 2 «Тренер у дома» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Поднять MVP-каркас спринта 1 до премиум-уровня по визуальному эталону `VISUAL-REFERENCE.html` и добавить две новые секции — премиум-карту района (`LocationMap`) и честный MVP записи (`BookingSlot`).

**Architecture:** База — `feat-spec-3` после merge `feat-spec-2` (Next.js 16 App Router, TS strict, Tailwind v4). Премиальность собирается через производные CSS-токены и `@layer components` (.card, .btn-primary, .btn-slot, .pill, .kicker), а не через утилитарный Tailwind россыпью (антипаттерн спринта 1). Существующие компоненты ре-стайлятся под новые классы; добавляются две новые секции и мостики Bridge между всеми секциями. Тесты — Vitest (TDD на BookingSlot и slots-data) + Playwright e2e на флоу.

**Tech Stack:** Next.js 16, React 19, TypeScript 5 strict, Tailwind v4, Vitest 3 + React Testing Library, Playwright 1.60, next/font (Gerhaus local + Nunito + JetBrains Mono).

---

## File Structure

| Файл | Действие | Ответственность |
|---|---|---|
| `app/globals.css` | переписать | Все токены, `@layer components` (.card, .btn-*, .pill, .kicker, .hairline), фон body, keyframes радара |
| `app/layout.tsx` | минор-правки | Подтвердить отсутствие inline `bg-` на body (фон через CSS) |
| `app/page.tsx` | переписать | Новый порядок секций + bridges + ре-стайл footer |
| `components/sections/Map.tsx` | **удалить** | Заменён LocationMap |
| `components/sections/LocationMap.tsx` | создать | Server: 3 колонки, SVG-карта, центр-зал, пины ЖК, CSS-радар |
| `components/sections/BookingSlot.tsx` | создать | Client: чипы слотов, выбор, TG deep link, без «забронировано» |
| `components/sections/Hero.tsx` | переписать | Премиум-кнопки, .pill, открытый блок на градиенте body |
| `components/sections/Problem.tsx` | переписать | .card, hairline между болями |
| `components/sections/Transformation.tsx` | переписать | .card, BAB с glow-акцентами |
| `components/sections/Objections.tsx` | переписать | .card, hairline между вопросами (тесты должны остаться зелёными) |
| `components/sections/FinalCta.tsx` | переписать | .card, .btn-primary + .btn-secondary |
| `components/quiz/Quiz.tsx` | переписать | .card на главную обёртку, .kicker |
| `components/quiz/QuizQuestion.tsx` | переписать | премиум-кнопки опций с hover-glow |
| `components/quiz/QuizResult.tsx` | переписать | премиум-карточка профиля с цветным glow |
| `components/quiz/ProgressBar.tsx` | переписать | премиум прогресс с glow |
| `components/ui/Pill.tsx` | переписать | использует `.pill` класс |
| `components/ui/GlassCard.tsx` | переписать | тонкая обёртка над `.card` (для backward-compat) |
| `components/ui/Bridge.tsx` | переписать | премиум pill + arrow, использует `.pill` |
| `components/ui/StickyCta.tsx` | переписать | премиум кнопка |
| `components/ui/TrackedLink.tsx` | проверить | убрать упоминание устаревшего `--glass` если есть |
| `components/ui/Reveal.tsx` | без изменений | IntersectionObserver обёртка |
| `lib/types.ts` | расширить | + `Slot`, `MapPoint`, Goal extend, `SiteContent.booking`, миграция `SiteContent.map` |
| `lib/slots-data.ts` | создать | плейсхолдеры с TODO |
| `lib/quiz-data.ts` | расширить | + `CONTENT.booking`, `BRIDGES.toBooking`, миграция `CONTENT.map` |
| `tests/components/BookingSlot.test.tsx` | создать | TDD: выбор, ссылка, цели, честность |
| `tests/lib/slots-data.test.ts` | создать | TDD: status=free, уникальность id |
| `tests/e2e/booking-slot.spec.ts` | создать | Полный сценарий выбор → TG |
| `tests/e2e/quiz-flow.spec.ts` | расширить | Bridge Quiz → #booking |
| `tests/e2e/reduced-motion.spec.ts` | расширить | Радар не анимируется |

---

## Phase 1 — Foundation (последовательно)

### Task 1: Переписать `app/globals.css` под рецепт §5 спеки

**Файлы:**
- Modify: `app/globals.css` (полная переписка)

- [ ] **Step 1: Заменить содержимое файла**

Замени всё содержимое `app/globals.css` на:

```css
@import "tailwindcss";

/* ── @theme: токены, которые Tailwind v4 превращает в утилиты ── */
@theme {
  --color-cyan:   #2CE6FF;
  --color-violet: #8B5CFF;
  --color-pink:   #FF4FD8;
  --color-green:  #36FF9D;
  --color-orange: #FF9F43;
  --color-blue:   #4D7DFF;

  --color-bg-primary: #0E1117;
  --color-bg2:        #151923;
  --color-bg3:        #1B2030;

  --color-tx:  #E8ECF4;
  --color-tx2: #9AA3B5;
  --color-tx3: #5E6678;

  /* Шрифтовые алиасы на next/font переменные */
  --font-display: var(--font-gerhaus);
  --font-body:    var(--font-nunito);
  --font-mono:    var(--font-jbm);
}

/* ── Кастомные переменные вне @theme (не нужны как Tailwind utility) ── */
:root {
  --radius-md: 22px;
  --radius-lg: 30px;
  --radius-xl: 42px;
  --blur-md:   18px;

  /* ПРОИЗВОДНЫЕ токены премиальности (рецепт §5.1 спеки) */
  --glass-bg-solid: #141925;
  --glass-bg: linear-gradient(180deg,
                              rgba(255,255,255,0.05) 0%,
                              rgba(255,255,255,0.02) 40%,
                              rgba(255,255,255,0.012) 100%);
  --line:      rgba(255,255,255,0.09);
  --line-soft: rgba(255,255,255,0.055);

  --edge-highlight: inset 0 1px 0 rgba(255,255,255,0.10);
  --lift: 0 1px 2px rgba(0,0,0,0.4),
          0 8px 24px rgba(0,0,0,0.35),
          0 24px 60px rgba(0,0,0,0.30);

  --glow-cyan:   0 0 14px 1px rgba(44,230,255,0.6);
  --glow-violet: 0 0 14px 1px rgba(139,92,255,0.6);
  --glow-pink:   0 0 14px 1px rgba(255,79,216,0.6);
  --glow-green:  0 0 14px 1px rgba(54,255,157,0.6);
  --glow-orange: 0 0 14px 1px rgba(255,159,67,0.6);

  --bg-gradient:
    radial-gradient(120% 80% at 50% -10%, rgba(139,92,255,0.16) 0%, rgba(139,92,255,0) 45%),
    radial-gradient(90% 60% at 80% 0%,   rgba(44,230,255,0.09) 0%, rgba(44,230,255,0) 50%),
    linear-gradient(180deg, #121624 0%, #0E1117 40%, #0A0D13 100%);
}

/* ── ПРИЁМ 1: фон body — градиентная атмосфера, fixed чтобы не плыл при скролле ── */
html {
  background: var(--color-bg-primary);
  color: var(--color-tx);
}
body {
  background-color: var(--color-bg-primary);
  background-image: var(--bg-gradient);
  background-attachment: fixed;
  color: var(--color-tx);
  font-family: var(--font-body), 'Nunito', 'Segoe UI', sans-serif;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  min-height: 100vh;
}

/* ── @layer components: премиум строительные блоки ── */
@layer components {
  /* ПРИЁМ 2: стеклянная карточка */
  .card {
    position: relative;
    background-color: var(--glass-bg-solid);
    background-image: var(--glass-bg);
    border: 1px solid var(--line);
    border-radius: var(--radius-xl);
    box-shadow: var(--edge-highlight), var(--lift);
    backdrop-filter: blur(var(--blur-md));
    padding: 34px 36px;
  }
  .card-lg { border-radius: var(--radius-xl); padding: 40px 44px; }
  .card-md { border-radius: var(--radius-lg); padding: 26px 28px; }
  .card-sm { border-radius: var(--radius-md); padding: 18px 20px; }

  /* ПРИЁМ 3: hairline-разделитель внутри карточки */
  .hairline { border-bottom: 1px solid var(--line-soft); }
  .hairline:last-child { border-bottom: none; }

  /* ПРИЁМ 5: кнопки */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    font-family: var(--font-body), 'Nunito', sans-serif;
    font-weight: 700;
    font-size: 16px;
    padding: 15px 28px;
    border-radius: 40px;
    cursor: pointer;
    text-decoration: none;
    border: none;
    transition: transform .15s ease, box-shadow .25s ease, opacity .15s ease;
  }
  .btn-primary {
    background: linear-gradient(100deg, var(--color-cyan), #20B8E8);
    color: #06121b;
    box-shadow: 0 0 0 1px rgba(44,230,255,0.5),
                0 8px 30px rgba(44,230,255,0.35);
  }
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 0 1px rgba(44,230,255,0.7),
                0 12px 44px rgba(44,230,255,0.5);
  }
  .btn-slot {
    background: linear-gradient(100deg, var(--color-violet), var(--color-blue) 50%, var(--color-cyan));
    color: #06121b;
    box-shadow: 0 0 0 1px rgba(139,92,255,0.4),
                0 12px 36px rgba(139,92,255,0.45);
  }
  .btn-slot:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 0 1px rgba(139,92,255,0.6),
                0 16px 48px rgba(139,92,255,0.55);
  }
  .btn-slot:disabled,
  .btn-slot[aria-disabled="true"] {
    opacity: 0.5;
    pointer-events: none;
    cursor: not-allowed;
  }
  .btn-secondary {
    background-color: rgba(255,255,255,0.02);
    background-image: var(--glass-bg);
    border: 1px solid var(--line);
    color: var(--color-tx);
    box-shadow: var(--edge-highlight);
  }
  .btn-secondary:hover { border-color: rgba(255,255,255,0.22); }

  /* пилюли (hero/booking/list-теги) */
  .pill {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-family: var(--font-mono), 'JetBrains Mono', monospace;
    font-size: 12.5px;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--color-tx2);
    padding: 9px 16px;
    border-radius: 30px;
    background: rgba(255,255,255,0.02);
    border: 1px solid var(--line);
    box-shadow: var(--edge-highlight);
  }

  /* типографические голоса */
  .kicker {
    font-family: var(--font-mono), 'JetBrains Mono', monospace;
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 2.6px;
    text-transform: uppercase;
    color: var(--color-cyan);
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
}

/* ── @keyframes (вне @layer — keyframes должны быть в top-level) ── */
@keyframes radar-pulse {
  0%   { transform: scale(0.6); opacity: 0.8; }
  100% { transform: scale(2);   opacity: 0; }
}
.radar-ring {
  transform-origin: center;
  animation: radar-pulse 3.2s ease-out infinite;
}
.radar-ring--d1 { animation-delay: 0s; }
.radar-ring--d2 { animation-delay: 1s; }
.radar-ring--d3 { animation-delay: 2s; }

/* ── Reduced motion: глушим радар, transition, reveal ── */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.001ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.001ms !important;
    scroll-behavior: auto !important;
  }
  .radar-ring {
    animation: none !important;
    opacity: 0.4;
  }
}
```

- [ ] **Step 2: Прогнать typecheck — он не должен ругаться (CSS его не касается)**

Run: `npm run typecheck`
Expected: чисто (`0 errors`).

- [ ] **Step 3: Прогнать dev и убедиться что страница не сломалась**

Run: `npm run dev`
Открой `http://localhost:3000` в браузере. Страница должна **отрисоваться без 500-ошибки** (визуально пока остаётся «плоской», т.к. секции ещё на старых классах, но фон body уже стал градиентным).

Останови dev (`Ctrl+C`).

- [ ] **Step 4: Commit**

```bash
git add app/globals.css
git commit -m "feat(css): premium tokens + @layer components (card, btn, pill, kicker)

Производные токены премиальности (--glass-bg, --line-soft, --lift,
--edge-highlight, --glow-*, --bg-gradient) и компонент-классы в
@layer components. Радар-keyframes вне layer (на top-level, иначе
ненадёжно собирается в Tailwind v4). Reduced-motion глушит радар.

Старый токен --glass удалён, но компоненты, которые его используют
(Pill/GlassCard/Bridge/Hero), будут переписаны в следующих задачах —
визуально они станут пока 'некрасивыми', тесты не падают.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 2: Проверить и причесать `app/layout.tsx`

Шрифты уже подключены через `next/font/local` (Gerhaus) и `next/font/google` (Nunito + JetBrainsMono) — этот шаг только удостоверяется, что нет конфликта с новым фоном body.

**Файлы:**
- Modify: `app/layout.tsx`

- [ ] **Step 1: Заменить содержимое файла**

Прочитай текущий файл, затем замени на:

```tsx
import type {Metadata, Viewport} from 'next';
import localFont from 'next/font/local';
import {Nunito, JetBrains_Mono} from 'next/font/google';
import {CONTENT} from '@/lib/quiz-data';
import {YandexMetrika} from '@/components/analytics/YandexMetrika';
import './globals.css';

const gerhaus = localFont({
    src: '../public/fonts/gerhaus/Gerhaus.ttf',
    variable: '--font-gerhaus',
    display: 'swap',
    fallback: ['Georgia', 'serif'],
});

const nunito = Nunito({
    subsets: ['latin', 'cyrillic'],
    weight: ['400', '600', '700', '800'],
    variable: '--font-nunito',
    display: 'swap',
});

const jbm = JetBrains_Mono({
    subsets: ['latin'],
    weight: ['400', '500'],
    variable: '--font-jbm',
    display: 'swap',
});

export const metadata: Metadata = {
    title: CONTENT.meta.title,
    description: CONTENT.meta.description,
    openGraph: {
        title: CONTENT.meta.ogTitle,
        description: CONTENT.meta.ogDescription,
        type: 'website',
        locale: 'ru_RU',
    },
};

export const viewport: Viewport = {
    themeColor: '#0E1117',
    width: 'device-width',
    initialScale: 1,
};

export default function RootLayout({children}: { children: React.ReactNode }) {
    return (
        <html
            lang="ru"
            className={`${gerhaus.variable} ${nunito.variable} ${jbm.variable} antialiased`}
        >
        <body className="min-h-screen flex flex-col">
        {children}
        <YandexMetrika/>
        </body>
        </html>
    );
}
```

**Что изменилось:** добавили `weight` к Nunito (400/600/700/800) и JetBrains Mono (400/500) — чтобы next/font реально загрузил эти веса. На `<body>` убрали `min-h-full` (заменили на `min-h-screen` — корректнее, т.к. `h-full` на html больше не нужен). На `<html>` убрали `h-full`.

- [ ] **Step 2: Запустить typecheck и lint**

Run: `npm run typecheck && npm run lint`
Expected: оба чисто.

- [ ] **Step 3: Commit**

```bash
git add app/layout.tsx
git commit -m "chore(layout): explicit font weights + min-h-screen on body

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 2 — Types + Data (последовательно)

### Task 3: Расширить `lib/types.ts` — Slot, MapPoint, Goal, booking, миграция SiteContent.map

Этот шаг ломает совместимость с `components/sections/Map.tsx` (он читает `CONTENT.map.points` в старом формате `{name, time}`). После этого шага и Task 5 (миграция данных) Map.tsx **временно не компилируется**. Это ок — Task 11 (LocationMap) и Task 19 (page.tsx) удалят его. Между Task 5 и Task 19 строго один промежуточный коммит (Task 11 — миграция Map → LocationMap, атомарная).

**Файлы:**
- Modify: `lib/types.ts`

- [ ] **Step 1: Заменить содержимое файла**

```ts
export type ProfileKey = 'health' | 'body' | 'energy';

export interface Profile {
    key: ProfileKey;
    badge: string;
    colorVar: string;
    title: string;
    body: string;
    offerTitle: string;
    offerText: string;
    tgMessage: string;
}

export interface QuizOption {
    label: string;
    sub: string;
    scores: ProfileKey;
}

export interface QuizQuestion {
    id: number;
    title: string;
    options: QuizOption[];
}

export interface BridgeContent {
    question: string;
    cta: string;
    href: string;
}

// ── НОВОЕ: точка ЖК на карте (SPEC §8) ──
export interface MapPoint {
    name: string;
    walkMinutes: number;
    /** CSS-переменная цвета акцента — определяет цвет dot/pin/glow */
    accentVar: '--color-cyan' | '--color-violet' | '--color-green' | '--color-pink';
    /** Координаты пина в % от viewbox SVG (0..100) */
    x: number;
    y: number;
}

// ── НОВОЕ: слот записи (SPEC §9) ──
// status — только 'free': busy не возим в DOM на MVP, чтобы не врать пользователю.
export interface Slot {
    id: string;
    day: string;
    time: string;
    label: string;
    status: 'free';
    profileHint?: ProfileKey;
    walkMinutes?: number;
}

export interface SiteContent {
    meta: {
        title: string;
        description: string;
        ogTitle: string;
        ogDescription: string;
    };
    hero: {
        kicker: string;
        h1: string;
        sub: string;
        pills: string[];
        cta: string;
        microcopy: string;
    };
    problem: {
        kicker: string;
        h2: string;
        cards: { emoji: string; title: string; text: string }[];
        summaryLead: string;
        summaryRest: string;
    };
    // ── МИГРАЦИЯ: новая структура для LocationMap ──
    map: {
        kicker: string;
        h2: string;
        body: string;
        gymLabel: string;
        points: MapPoint[];
    };
    transformation: {
        kicker: string;
        h2: string;
        before: string[];
        bridge: string;
        after: string;
        afterItems: string[];
    };
    quiz: {
        kicker: string;
        h2: string;
        sub: string;
        resultNote: string;
        restart: string;
    };
    // ── НОВОЕ: контент BookingSlot ──
    booking: {
        kicker: string;
        h2: string;
        body: string;
        pillGym: string;
        ctaLabel: string;
        honestyNote: string;
    };
    objections: {
        kicker: string;
        h2: string;
        items: { q: string; a: string }[];
    };
    finalCta: {
        kicker: string;
        h2: string;
        text: string;
        cta1: string;
        cta2: string;
        guarantee: string;
    };
    footer: { name: string; tagline: string };
    stickyCta: string;
}

export interface QuizState {
    step: number;
    scores: Record<ProfileKey, number>;
    finished: boolean;
    result: ProfileKey | null;
}

// ── Аналитика: добавлены slot_select / slot_take ──
export type Goal =
    | 'quiz_start'
    | 'quiz_complete'
    | `lead_click_${ProfileKey}`
    | 'lead_click_direct'
    | 'slot_select'
    | 'slot_take'
    | 'objection_open';

// ── Фаза 2: payload заявки (не используется в MVP) ──
export interface LeadPayload {
    name: string;
    contact: string;
    profile?: ProfileKey;
    slotId?: string;
    source: 'quiz' | 'direct' | 'slot';
}
```

- [ ] **Step 2: НЕ запускать typecheck сейчас**

Сейчас typecheck **упадёт** на `components/sections/Map.tsx` (он читает старый формат `points: { name, time }`) и на `lib/quiz-data.ts` (CONTENT.map имеет старую форму). Это ожидаемо — фиксим в Task 5 и Task 11.

- [ ] **Step 3: Commit**

```bash
git add lib/types.ts
git commit -m "feat(types): Slot, MapPoint, booking content, slot goals (SPEC §4)

Расширение типов под спринт 2. Goal union пополнен slot_select/
slot_take (SPEC §9.3). SiteContent.map переключён на новую структуру
с MapPoint[] вместо {name, time}; SiteContent.booking — для секции
BookingSlot. Старый LeadPayload расширен полями slotId/source под
фазу 2.

Это коммит ломает компиляцию components/sections/Map.tsx и текущий
CONTENT.map — фикс в следующих задачах (Task 5 + Task 11).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 4: TDD — `lib/slots-data.ts` + `tests/lib/slots-data.test.ts`

**Файлы:**
- Create: `tests/lib/slots-data.test.ts`
- Create: `lib/slots-data.ts`

- [ ] **Step 1: Написать тесты (RED)**

Создай `tests/lib/slots-data.test.ts`:

```ts
import {describe, it, expect} from 'vitest';
import {SLOTS} from '@/lib/slots-data';

describe('SLOTS data (SPEC §9.4)', () => {
    it('все слоты со status === "free" (busy на MVP не возим в DOM)', () => {
        for (const s of SLOTS) {
            expect(s.status).toBe('free');
        }
    });

    it('все id уникальны', () => {
        const ids = SLOTS.map((s) => s.id);
        expect(new Set(ids).size).toBe(ids.length);
    });

    it('у каждого слота непустые day/time/label', () => {
        for (const s of SLOTS) {
            expect(s.day).toBeTruthy();
            expect(s.time).toBeTruthy();
            expect(s.label).toBeTruthy();
        }
    });

    it('массив непустой (минимум один слот, иначе секция бесполезна)', () => {
        expect(SLOTS.length).toBeGreaterThan(0);
    });
});
```

- [ ] **Step 2: Запустить — должно упасть на отсутствии модуля**

Run: `npm test -- tests/lib/slots-data.test.ts`
Expected: FAIL, ошибка `Cannot find module '@/lib/slots-data'`.

- [ ] **Step 3: Создать `lib/slots-data.ts`**

```ts
import type {Slot} from './types';

// TODO: согласовать с тренером перед продом — НЕ выдумывать «свободно».
// На MVP допустимы ТОЛЬКО реально открытые окна (SPEC §9.4).
export const SLOTS: Slot[] = [
    {
        id: 'tue-19',
        day: 'Вторник',
        time: '19:00',
        label: 'Персональная · 60 мин · зал у дома',
        status: 'free',
        walkMinutes: 4,
    },
    {
        id: 'wed-08',
        day: 'Среда',
        time: '08:00',
        label: 'Персональная · 60 мин · зал у дома',
        status: 'free',
        walkMinutes: 4,
        profileHint: 'energy',
    },
    {
        id: 'thu-19',
        day: 'Четверг',
        time: '19:00',
        label: 'Персональная · 60 мин · зал у дома',
        status: 'free',
        walkMinutes: 4,
        profileHint: 'health',
    },
    {
        id: 'sat-10',
        day: 'Суббота',
        time: '10:00',
        label: 'Парная · 60 мин · зал у дома',
        status: 'free',
        walkMinutes: 4,
        profileHint: 'body',
    },
    {
        id: 'sun-11',
        day: 'Воскресенье',
        time: '11:00',
        label: 'Персональная · 60 мин · зал у дома',
        status: 'free',
        walkMinutes: 4,
    },
];
```

- [ ] **Step 4: Запустить тесты — должны проходить**

Run: `npm test -- tests/lib/slots-data.test.ts`
Expected: 4/4 PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/slots-data.ts tests/lib/slots-data.test.ts
git commit -m "feat(slots): placeholder slots-data + tests (SPEC §9.4)

5 демо-слотов, все status:'free'. TODO в коде помечает что данные
заменяются на реальные перед продом. Тесты гарантируют что 'busy'
случайно не попадёт в массив (защита от компрометации честности UI).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 5: Расширить `lib/quiz-data.ts` — миграция CONTENT.map + CONTENT.booking + BRIDGES.toBooking

**Файлы:**
- Modify: `lib/quiz-data.ts`

- [ ] **Step 1: Заменить `BRIDGES` константу — добавить toBooking**

Найди в `lib/quiz-data.ts` блок `export const BRIDGES` и замени на:

```ts
/** Мостики между секциями (вопрос + кнопка + якорь). SPEC §6. */
export const BRIDGES: Record<string, BridgeContent> = {
    toProblem: {question: 'Знакомо: вроде начинаешь — и снова бросаешь?', cta: 'да, узнаю себя', href: '#pain'},
    toMap: {question: 'А что, если до зала — пять минут пешком?', cta: 'показать на карте', href: '#map'},
    toTransformation: {question: 'И как это меняет жизнь на дистанции?', cta: 'смотреть до / после', href: '#bab'},
    toQuiz: {question: 'С чего начать именно тебе? Давай разберёмся за минуту.', cta: 'пройти разбор', href: '#test'},
    toBooking: {question: 'Тогда давай прямо сейчас выберем время.', cta: 'выбрать слот', href: '#booking'},
    toObjections: {question: 'Остались сомнения? Это нормально.', cta: 'снять сомнения', href: '#objections'},
};
```

- [ ] **Step 2: Заменить блок `map:` внутри `CONTENT` на новую структуру**

Найди в `lib/quiz-data.ts` объект `map: { ... }` внутри `CONTENT` и замени на:

```ts
    map: {
        kicker: 'ЗАЛ У ДОМА — УДОБНО И РЕАЛЬНО',
        h2: 'Тренировки рядом с вами',
        body: 'Тренажёрный зал на Приморском проспекте, 56. От большинства ЖК района — 4-10 минут пешком. Это и есть главная причина не сорваться.',
        gymLabel: 'ЗАЛ · ПРИМОРСКИЙ ПР., 56',
        // TODO: сверить минуты и названия ЖК по Яндекс.Картам перед продом.
        points: [
            {name: 'ЖК Морской каскад', walkMinutes: 4, accentVar: '--color-cyan', x: 30, y: 28},
            {name: 'ЖК Светлый мир Внутри', walkMinutes: 6, accentVar: '--color-violet', x: 72, y: 36},
            {name: 'ЖК Промикс', walkMinutes: 8, accentVar: '--color-green', x: 22, y: 70},
            {name: 'ЖК Сампсониевский', walkMinutes: 10, accentVar: '--color-cyan', x: 78, y: 76},
        ],
    },
```

- [ ] **Step 3: Добавить блок `booking:` внутри `CONTENT` (после `quiz:`, перед `objections:`)**

```ts
    booking: {
        kicker: 'ВЫБЕРИ УДОБНОЕ ВРЕМЯ',
        h2: 'Запишемся прямо сейчас',
        body: 'Свободные окна на ближайшие дни. Жми на удобное — продолжим в Telegram.',
        pillGym: 'зал у дома · Приморский, 56',
        ctaLabel: 'Занять слот',
        honestyNote: 'Откроется Telegram с выбранным временем — подтвердим лично.',
    },
```

- [ ] **Step 4: Запустить typecheck**

Run: `npm run typecheck`

Expected: ошибки **только** в `components/sections/Map.tsx` (читает старые поля `time`, `center`, `caption`, `sub`). Это ожидаемо — Map.tsx удаляется в Task 11.

- [ ] **Step 5: Commit**

```bash
git add lib/quiz-data.ts
git commit -m "feat(content): migrate CONTENT.map + add CONTENT.booking + toBooking bridge

CONTENT.map переключён на MapPoint[] (под LocationMap). CONTENT.booking
содержит kicker/h2/body/pillGym/ctaLabel/honestyNote для BookingSlot.
BRIDGES.toBooking — мостик Quiz → BookingSlot.

components/sections/Map.tsx сломан — будет удалён в Task 11.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 3 — UI-кит ре-стайл

### Task 6: Переписать `components/ui/Pill.tsx`

**Файлы:**
- Modify: `components/ui/Pill.tsx`

- [ ] **Step 1: Заменить содержимое файла**

```tsx
import type {ReactNode} from 'react';

/**
 * Премиум-пилюля — обёртка над .pill из @layer components (globals.css).
 * Используется в Hero (метки тренера), BookingSlot (тег «зал у дома»),
 * списках точек ЖК и пр.
 */
export function Pill({children}: { children: ReactNode }) {
    return <span className="pill">{children}</span>;
}
```

- [ ] **Step 2: Прогнать typecheck — Pill потребителей не ломает**

Run: `npm run typecheck`
Expected: ошибки только в `Map.tsx` (как в Task 5).

- [ ] **Step 3: Commit**

```bash
git add components/ui/Pill.tsx
git commit -m "refactor(ui/Pill): use .pill class from @layer components

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 7: Переписать `components/ui/GlassCard.tsx`

**Файлы:**
- Modify: `components/ui/GlassCard.tsx`

- [ ] **Step 1: Заменить содержимое файла**

```tsx
import type {ReactNode} from 'react';

/**
 * Премиум-карточка — тонкая обёртка над .card из @layer components.
 * Принимает optional variant: 'sm' | 'md' | 'lg' | 'xl' (default 'xl' = .card).
 */
type Variant = 'sm' | 'md' | 'lg' | 'xl';

const variantClass: Record<Variant, string> = {
    sm: 'card-sm',
    md: 'card-md',
    lg: 'card-lg',
    xl: 'card',
};

export function GlassCard({
    children,
    className = '',
    variant = 'xl',
}: {
    children: ReactNode;
    className?: string;
    variant?: Variant;
}) {
    return (
        <div className={`${variantClass[variant]} ${className}`.trim()}>
            {children}
        </div>
    );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/ui/GlassCard.tsx
git commit -m "refactor(ui/GlassCard): wrap .card variants (sm/md/lg/xl)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 8: Переписать `components/ui/Bridge.tsx`

**Файлы:**
- Modify: `components/ui/Bridge.tsx`

- [ ] **Step 1: Заменить содержимое файла**

```tsx
import type {BridgeContent} from '@/lib/types';

/**
 * Мостик между секциями: короткий вопрос + .pill-кнопка-якорь.
 * SPEC §6. Премиум-стиль через .pill и hover-glow.
 */
export function Bridge({data}: { data: BridgeContent }) {
    return (
        <div className="my-16 flex flex-col items-center gap-4 px-4 text-center md:my-20">
            <p className="font-display text-xl uppercase tracking-tight text-tx2 md:text-2xl">
                {data.question}
            </p>
            <a
                href={data.href}
                className="pill transition-colors hover:!border-cyan hover:!text-cyan"
                style={{cursor: 'pointer'}}
            >
                {data.cta} <span aria-hidden>→</span>
            </a>
        </div>
    );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/ui/Bridge.tsx
git commit -m "refactor(ui/Bridge): premium .pill anchor + tight tracking

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 9: Проверить и причесать `components/ui/TrackedLink.tsx`

**Файлы:**
- Modify (возможно): `components/ui/TrackedLink.tsx`

- [ ] **Step 1: Прочитать файл**

Run: `cat components/ui/TrackedLink.tsx`

- [ ] **Step 2: Если в файле упоминается `bg-[--glass]` или `border-[--line]` — оставить как есть**

`TrackedLink` — это logical wrapper над `<a>` с `reachGoal` перед навигацией. Если он содержит стилевые классы — оставить их (стили задаёт вызывающий компонент через `className`). Если есть встроенный `bg-[--glass]` — удалить, чтобы вызывающий полностью контролировал стиль через свой className.

**Целевое содержимое** (если требуется правка):

```tsx
'use client';

import type {ReactNode, MouseEvent, AnchorHTMLAttributes} from 'react';
import type {Goal} from '@/lib/types';
import {reachGoal} from '@/lib/analytics';

type Props = Omit<AnchorHTMLAttributes<HTMLAnchorElement>, 'onClick'> & {
    goal: Goal;
    children: ReactNode;
    external?: boolean;
    onClick?: (e: MouseEvent<HTMLAnchorElement>) => void;
};

/**
 * Якорь, который перед навигацией шлёт reachGoal(goal).
 * Не задаёт собственных визуальных стилей — оставляет это вызывающему.
 */
export function TrackedLink({
    goal,
    children,
    external = false,
    onClick,
    ...rest
}: Props) {
    const handle = (e: MouseEvent<HTMLAnchorElement>) => {
        reachGoal(goal);
        onClick?.(e);
    };
    const externalAttrs = external ? {target: '_blank', rel: 'noopener noreferrer'} : {};
    return (
        <a {...rest} {...externalAttrs} onClick={handle}>
            {children}
        </a>
    );
}
```

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`
Expected: ошибки только в Map.tsx.

- [ ] **Step 4: Commit (если файл менялся)**

```bash
git add components/ui/TrackedLink.tsx
git commit -m "refactor(ui/TrackedLink): clean stylistic concerns, delegate visuals to caller

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

Если файл не менялся — пропусти commit.

---

### Task 10: Переписать `components/ui/StickyCta.tsx`

**Файлы:**
- Modify: `components/ui/StickyCta.tsx`

- [ ] **Step 1: Прочитать текущий файл**

Run: `cat components/ui/StickyCta.tsx`

Запомни: компонент `'use client'`, рендерит mobile-only кнопку, scroll к `#test`, использует `CONTENT.stickyCta`.

- [ ] **Step 2: Заменить содержимое**

```tsx
'use client';

import {useEffect, useState} from 'react';
import {CONTENT} from '@/lib/quiz-data';

/**
 * Sticky-CTA внизу экрана на мобилке: появляется после небольшого
 * скролла, ведёт к якорю #test (квиз). SPEC §13.
 */
export function StickyCta() {
    const [visible, setVisible] = useState(false);

    useEffect(() => {
        const onScroll = () => setVisible(window.scrollY > 240);
        onScroll();
        window.addEventListener('scroll', onScroll, {passive: true});
        return () => window.removeEventListener('scroll', onScroll);
    }, []);

    return (
        <div
            className={`pointer-events-none fixed inset-x-0 bottom-3 z-40 flex justify-center px-4 transition-opacity duration-200 md:hidden ${
                visible ? 'opacity-100' : 'opacity-0'
            }`}
            aria-hidden={!visible}
        >
            <a
                href="#test"
                className="btn btn-primary pointer-events-auto w-full max-w-md"
            >
                {CONTENT.stickyCta}
            </a>
        </div>
    );
}
```

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`
Expected: ошибки только в Map.tsx.

- [ ] **Step 4: Прогнать e2e тест mobile-sticky чтобы убедиться что регрессии нет**

Run: `npx playwright install --with-deps chromium` (один раз, если не установлен)
Run: `npm run test:e2e -- mobile-sticky`
Expected: PASS (тест проверяет видимость и переход, оба сохранены).

- [ ] **Step 5: Commit**

```bash
git add components/ui/StickyCta.tsx
git commit -m "refactor(ui/StickyCta): use .btn.btn-primary, retain scroll-trigger

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 4 — Новые секции

### Task 11: Миграция Map → LocationMap (атомарная)

Эта задача атомарно: создаёт `LocationMap`, заменяет в `page.tsx`, удаляет старый `Map.tsx`. После неё typecheck снова чистый.

**Файлы:**
- Create: `components/sections/LocationMap.tsx`
- Modify: `app/page.tsx` (точечно — только импорт и тег)
- Delete: `components/sections/Map.tsx`

- [ ] **Step 1: Создать `components/sections/LocationMap.tsx`**

```tsx
import {CONTENT} from '@/lib/quiz-data';
import type {MapPoint} from '@/lib/types';

/**
 * Премиум-карта района (SPEC §8). Server Component.
 * 3 колонки на десктопе, стек на мобиле.
 * Центр — стеклянная квадратная карта с SVG-подложкой и CSS-радаром.
 */
export function LocationMap() {
    const {kicker, h2, body, gymLabel, points} = CONTENT.map;

    return (
        <section
            id="map"
            className="relative mx-auto w-full max-w-6xl px-4 py-20 md:py-28"
        >
            <div className="grid gap-10 md:grid-cols-[1fr_minmax(360px,460px)_1fr] md:items-center md:gap-12">
                {/* ── Левая колонка ── */}
                <div className="flex flex-col gap-4">
                    <span className="kicker">◆ {kicker}</span>
                    <h2 className="font-display text-3xl uppercase tracking-tight text-tx md:text-4xl">
                        {h2}
                    </h2>
                    <p className="max-w-md text-base text-tx2">{body}</p>
                </div>

                {/* ── Центр: квадратная карта ── */}
                <div className="card aspect-square w-full p-4 md:p-6">
                    <MapSvg points={points} gymLabel={gymLabel}/>
                </div>

                {/* ── Правая колонка: список ЖК ── */}
                <ul className="flex flex-col gap-3">
                    {points.map((p) => (
                        <li
                            key={p.name}
                            className="card-sm hairline flex items-center gap-3"
                            style={{borderRadius: 'var(--radius-md)'}}
                        >
                            <span
                                className="inline-block h-3 w-3 shrink-0 rounded-full"
                                style={{
                                    backgroundColor: `var(${p.accentVar})`,
                                    boxShadow: glowForAccent(p.accentVar),
                                }}
                                aria-hidden
                            />
                            <span className="flex-1 text-sm text-tx">{p.name}</span>
                            <span className="font-mono text-xs text-tx3">~{p.walkMinutes} мин</span>
                        </li>
                    ))}
                </ul>
            </div>
        </section>
    );
}

function MapSvg({points, gymLabel}: { points: MapPoint[]; gymLabel: string }) {
    return (
        <div className="relative h-full w-full">
            <svg
                viewBox="0 0 100 100"
                preserveAspectRatio="xMidYMid meet"
                className="absolute inset-0 h-full w-full"
                aria-label="Схема расположения зала и ближайших ЖК"
            >
                {/* Сетка улиц (приглушённая) */}
                {Array.from({length: 9}).map((_, i) => (
                    <line
                        key={`h-${i}`}
                        x1={0}
                        y1={(i + 1) * 10}
                        x2={100}
                        y2={(i + 1) * 10}
                        stroke="rgba(255,255,255,0.04)"
                        strokeWidth={0.4}
                    />
                ))}
                {Array.from({length: 9}).map((_, i) => (
                    <line
                        key={`v-${i}`}
                        x1={(i + 1) * 10}
                        y1={0}
                        x2={(i + 1) * 10}
                        y2={100}
                        stroke="rgba(255,255,255,0.04)"
                        strokeWidth={0.4}
                    />
                ))}
                {/* Диагональный «проспект» */}
                <line
                    x1={5}
                    y1={95}
                    x2={95}
                    y2={5}
                    stroke="rgba(139,92,255,0.18)"
                    strokeWidth={1.4}
                    strokeLinecap="round"
                />

                {/* Радар: 3 кольца с задержкой */}
                <circle cx={50} cy={50} r={10} fill="none" stroke="rgba(44,230,255,0.5)" strokeWidth={0.6}
                        className="radar-ring radar-ring--d1"/>
                <circle cx={50} cy={50} r={10} fill="none" stroke="rgba(139,92,255,0.4)" strokeWidth={0.6}
                        className="radar-ring radar-ring--d2"/>
                <circle cx={50} cy={50} r={10} fill="none" stroke="rgba(255,79,216,0.3)" strokeWidth={0.6}
                        className="radar-ring radar-ring--d3"/>

                {/* Пины ЖК */}
                {points.map((p) => (
                    <g key={p.name}>
                        <circle
                            cx={p.x}
                            cy={p.y}
                            r={2.6}
                            fill={`var(${p.accentVar})`}
                            style={{filter: `drop-shadow(0 0 4px ${rgbaForAccent(p.accentVar, 0.85)})`}}
                        />
                    </g>
                ))}

                {/* Центр: бейдж-зал */}
                <g>
                    <circle
                        cx={50}
                        cy={50}
                        r={6}
                        fill="url(#gymGradient)"
                        style={{filter: 'drop-shadow(0 0 8px rgba(44,230,255,0.7))'}}
                    />
                    <text
                        x={50}
                        y={51.5}
                        textAnchor="middle"
                        fontSize={4}
                        fontWeight={700}
                        fill="#06121b"
                        fontFamily="var(--font-display), Georgia, serif"
                    >
                        K
                    </text>
                </g>

                <defs>
                    <linearGradient id="gymGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                        <stop offset="0%" stopColor="#8B5CFF"/>
                        <stop offset="100%" stopColor="#2CE6FF"/>
                    </linearGradient>
                </defs>
            </svg>

            {/* Подпись зала под SVG */}
            <p
                className="absolute inset-x-0 bottom-1 text-center font-mono text-[10px] uppercase tracking-[0.18em] text-tx3 md:text-xs"
                aria-hidden
            >
                {gymLabel}
            </p>
        </div>
    );
}

function glowForAccent(v: MapPoint['accentVar']): string {
    switch (v) {
        case '--color-cyan':
            return 'var(--glow-cyan)';
        case '--color-violet':
            return 'var(--glow-violet)';
        case '--color-green':
            return 'var(--glow-green)';
        case '--color-pink':
            return 'var(--glow-pink)';
    }
}

function rgbaForAccent(v: MapPoint['accentVar'], a: number): string {
    switch (v) {
        case '--color-cyan':
            return `rgba(44,230,255,${a})`;
        case '--color-violet':
            return `rgba(139,92,255,${a})`;
        case '--color-green':
            return `rgba(54,255,157,${a})`;
        case '--color-pink':
            return `rgba(255,79,216,${a})`;
    }
}
```

- [ ] **Step 2: Удалить старый Map.tsx**

Run: `git rm components/sections/Map.tsx`

- [ ] **Step 3: Обновить импорт в `app/page.tsx`**

Найди в `app/page.tsx` строку:
```tsx
import { Map } from '@/components/sections/Map';
```
Замени на:
```tsx
import {LocationMap} from '@/components/sections/LocationMap';
```

И найди:
```tsx
<Map />
```
Замени на:
```tsx
<LocationMap />
```

- [ ] **Step 4: Прогнать typecheck**

Run: `npm run typecheck`
Expected: чисто (0 ошибок).

- [ ] **Step 5: Прогнать build**

Run: `npm run build`
Expected: build проходит без ошибок (с предупреждениями о размерах ок).

- [ ] **Step 6: Прогнать существующие тесты — не должны падать (LocationMap не покрыт unit-тестами, это норма)**

Run: `npm test`
Expected: все тесты PASS (Map тестов не было).

- [ ] **Step 7: Commit**

```bash
git add components/sections/LocationMap.tsx app/page.tsx
git commit -m "feat(sections): LocationMap (premium SVG map + CSS radar), replace Map

SPEC §8. Server Component. 3 колонки → стек на мобиле. Центр —
квадратная стеклянная карта с SVG: сетка улиц, диагональ-проспект,
радар из 3 колец (CSS animation, выкл при reduced-motion), центр-
бейдж (градиент violet→cyan + glow), пины ЖК (цвет/glow из
MapPoint.accentVar). Правая колонка — список ЖК со временем (моно).

Старый плоский Map.tsx удалён.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 12: TDD — `tests/components/BookingSlot.test.tsx` + `components/sections/BookingSlot.tsx`

**Файлы:**
- Create: `tests/components/BookingSlot.test.tsx`
- Create: `components/sections/BookingSlot.tsx`

- [ ] **Step 1: Написать тесты (RED)**

Создай `tests/components/BookingSlot.test.tsx`:

```tsx
import {describe, it, expect, beforeEach, afterEach, vi} from 'vitest';
import {render, screen, within} from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import {BookingSlot} from '@/components/sections/BookingSlot';
import {SLOTS} from '@/lib/slots-data';

describe('BookingSlot (SPEC §9)', () => {
    beforeEach(() => {
        vi.stubEnv('NEXT_PUBLIC_YM_ID', '99999999');
        vi.stubEnv('NEXT_PUBLIC_TG_USERNAME', 'test_user');
    });
    afterEach(() => {
        vi.unstubAllEnvs();
        delete window.ym;
    });

    it('рендерит все слоты как кнопки с aria-pressed="false"', () => {
        render(<BookingSlot/>);
        const chips = screen.getAllByRole('button', {name: /\d{2}:\d{2}/});
        expect(chips).toHaveLength(SLOTS.length);
        for (const c of chips) {
            expect(c).toHaveAttribute('aria-pressed', 'false');
        }
    });

    it('CTA «Занять слот» disabled пока ничего не выбрано', () => {
        render(<BookingSlot/>);
        const cta = screen.getByTestId('booking-cta');
        expect(cta).toHaveAttribute('aria-disabled', 'true');
    });

    it('подпись честности про Telegram всегда видна', () => {
        render(<BookingSlot/>);
        expect(screen.getByText(/откроется telegram/i)).toBeInTheDocument();
    });

    it('выбор чипа: aria-pressed становится "true", CTA становится включённым', async () => {
        const user = userEvent.setup();
        render(<BookingSlot/>);
        const chip = screen.getAllByRole('button', {name: /\d{2}:\d{2}/})[0];
        await user.click(chip);
        expect(chip).toHaveAttribute('aria-pressed', 'true');
        const cta = screen.getByTestId('booking-cta');
        expect(cta).not.toHaveAttribute('aria-disabled', 'true');
    });

    it('выбор чипа: href ссылки CTA содержит выбранный день и время (encoded)', async () => {
        const user = userEvent.setup();
        render(<BookingSlot/>);
        const target = SLOTS[1];
        const chip = screen.getByRole('button', {name: new RegExp(target.time)});
        await user.click(chip);
        const cta = screen.getByTestId('booking-cta') as HTMLAnchorElement;
        expect(cta.href).toContain('t.me/test_user');
        expect(decodeURIComponent(cta.href)).toContain(target.day);
        expect(decodeURIComponent(cta.href)).toContain(target.time);
    });

    it('выбор чипа шлёт reachGoal("slot_select")', async () => {
        const ym = vi.fn();
        window.ym = ym;
        const user = userEvent.setup();
        render(<BookingSlot/>);
        await user.click(screen.getAllByRole('button', {name: /\d{2}:\d{2}/})[0]);
        const calls = ym.mock.calls.filter((c) => c[2] === 'slot_select');
        expect(calls).toHaveLength(1);
    });

    it('клик по активному CTA шлёт reachGoal("slot_take")', async () => {
        const ym = vi.fn();
        window.ym = ym;
        const user = userEvent.setup();
        render(<BookingSlot/>);
        await user.click(screen.getAllByRole('button', {name: /\d{2}:\d{2}/})[0]);
        await user.click(screen.getByTestId('booking-cta'));
        const calls = ym.mock.calls.filter((c) => c[2] === 'slot_take');
        expect(calls).toHaveLength(1);
    });

    it('честность: после клика «Занять» в DOM нет слов забронировано/занято/зарезервировано', async () => {
        const user = userEvent.setup();
        render(<BookingSlot/>);
        await user.click(screen.getAllByRole('button', {name: /\d{2}:\d{2}/})[0]);
        await user.click(screen.getByTestId('booking-cta'));
        const html = document.body.innerHTML.toLowerCase();
        for (const word of ['забронирован', 'занято', 'зарезервирован', 'ваш слот', 'место за вами']) {
            expect(html).not.toContain(word);
        }
    });

    it('выбор второго чипа: первый снова aria-pressed="false"', async () => {
        const user = userEvent.setup();
        render(<BookingSlot/>);
        const chips = screen.getAllByRole('button', {name: /\d{2}:\d{2}/});
        await user.click(chips[0]);
        await user.click(chips[1]);
        expect(chips[0]).toHaveAttribute('aria-pressed', 'false');
        expect(chips[1]).toHaveAttribute('aria-pressed', 'true');
    });

    it('секция имеет id="booking" (якорь для bridge)', () => {
        const {container} = render(<BookingSlot/>);
        expect(container.querySelector('#booking')).not.toBeNull();
    });
});
```

- [ ] **Step 2: Запустить тесты — все должны упасть на отсутствии модуля**

Run: `npm test -- tests/components/BookingSlot.test.tsx`
Expected: FAIL, `Cannot find module '@/components/sections/BookingSlot'`.

- [ ] **Step 3: Создать `components/sections/BookingSlot.tsx`**

```tsx
'use client';

import {useMemo, useState} from 'react';
import {SLOTS} from '@/lib/slots-data';
import {CONTENT} from '@/lib/quiz-data';
import {buildTelegramLink} from '@/lib/telegram';
import {reachGoal} from '@/lib/analytics';
import type {ProfileKey} from '@/lib/types';

const profileGlow: Record<ProfileKey, string> = {
    health: 'var(--glow-green)',
    body: 'var(--glow-pink)',
    energy: 'var(--glow-orange)',
};
const profileColor: Record<ProfileKey, string> = {
    health: 'var(--color-green)',
    body: 'var(--color-pink)',
    energy: 'var(--color-orange)',
};

/**
 * Секция записи (SPEC §9). Честный MVP:
 * — выбор чипа → ссылка в Telegram с текстом «удобное время: …».
 * — НЕ резервирует слот, НЕТ статуса «забронировано» в DOM.
 */
export function BookingSlot() {
    const {kicker, h2, body, pillGym, ctaLabel, honestyNote} = CONTENT.booking;
    const [selectedId, setSelectedId] = useState<string | null>(null);

    const selected = useMemo(
        () => SLOTS.find((s) => s.id === selectedId) ?? null,
        [selectedId]
    );

    const tgHref = selected
        ? buildTelegramLink(
              `Привет! Хочу записаться на персональную тренировку. ` +
              `Удобное время: ${selected.day}, ${selected.time}. ` +
              `Зал у дома (Приморский пр., 56).`
          )
        : undefined;

    const handlePick = (id: string) => {
        setSelectedId(id);
        reachGoal('slot_select');
    };

    const handleTake = () => {
        if (!selected) return;
        reachGoal('slot_take');
    };

    return (
        <section
            id="booking"
            className="mx-auto w-full max-w-3xl px-4 py-20 md:py-28"
        >
            <div className="card">
                <div className="flex flex-col gap-3">
                    <span className="kicker">◆ {kicker}</span>
                    <h2 className="font-display text-3xl uppercase tracking-tight text-tx md:text-4xl">
                        {h2}
                    </h2>
                    <p className="text-base text-tx2">{body}</p>
                    <div className="mt-2">
                        <span className="pill">{pillGym}</span>
                    </div>
                </div>

                <div
                    className="mt-8 grid gap-3"
                    style={{gridTemplateColumns: 'repeat(auto-fit, minmax(170px, 1fr))'}}
                >
                    {SLOTS.map((slot) => {
                        const isActive = slot.id === selectedId;
                        return (
                            <button
                                key={slot.id}
                                type="button"
                                aria-pressed={isActive}
                                onClick={() => handlePick(slot.id)}
                                className="card-sm relative flex flex-col items-start gap-1.5 text-left transition-all hover:!border-cyan"
                                style={
                                    isActive
                                        ? {
                                              boxShadow:
                                                  '0 0 0 2px var(--color-cyan), var(--glow-cyan), var(--edge-highlight), var(--lift)',
                                              borderColor: 'var(--color-cyan)',
                                          }
                                        : undefined
                                }
                            >
                                <div className="flex w-full items-center justify-between gap-2">
                                    <span className="font-mono text-sm text-tx">
                                        {slot.day} · {slot.time}
                                    </span>
                                    {slot.profileHint && (
                                        <span
                                            className="inline-block h-2 w-2 shrink-0 rounded-full"
                                            style={{
                                                backgroundColor: profileColor[slot.profileHint],
                                                boxShadow: profileGlow[slot.profileHint],
                                            }}
                                            aria-label={`профиль ${slot.profileHint}`}
                                        />
                                    )}
                                </div>
                                <span className="text-xs text-tx3">{slot.label}</span>
                                {typeof slot.walkMinutes === 'number' && (
                                    <span className="font-mono text-[11px] text-tx3">
                                        ~{slot.walkMinutes} мин пешком
                                    </span>
                                )}
                            </button>
                        );
                    })}
                </div>

                <div className="mt-8 flex flex-col items-stretch gap-3">
                    {selected && tgHref ? (
                        <a
                            data-testid="booking-cta"
                            href={tgHref}
                            target="_blank"
                            rel="noopener noreferrer"
                            onClick={handleTake}
                            className="btn btn-slot w-full"
                        >
                            {ctaLabel} {selected.day} · {selected.time} →
                        </a>
                    ) : (
                        <button
                            data-testid="booking-cta"
                            type="button"
                            aria-disabled="true"
                            className="btn btn-slot w-full"
                        >
                            {ctaLabel} →
                        </button>
                    )}
                    <p className="text-center font-mono text-[11px] uppercase tracking-[0.14em] text-tx3">
                        {honestyNote}
                    </p>
                </div>
            </div>
        </section>
    );
}
```

- [ ] **Step 4: Запустить тесты — должны проходить**

Run: `npm test -- tests/components/BookingSlot.test.tsx`
Expected: 10/10 PASS.

- [ ] **Step 5: Прогнать ВСЕ unit-тесты — регрессии не должно быть**

Run: `npm test`
Expected: все тесты PASS (включая `Quiz`, `Objections`, `quiz-logic`, `telegram`, `analytics`, `slots-data`, `BookingSlot`).

- [ ] **Step 6: Commit**

```bash
git add components/sections/BookingSlot.tsx tests/components/BookingSlot.test.tsx
git commit -m "feat(sections): BookingSlot — honest MVP slot picker (SPEC §9)

— Чипы слотов (aria-pressed), выбор шлёт slot_select.
— CTA <a> с deep link в TG, содержит выбранный день/время; клик
  шлёт slot_take.
— Кнопка disabled (aria-disabled) пока ничего не выбрано.
— В DOM никогда не появляется 'забронировано'/'занято'/etc — это
  проверено отдельным тестом (защита §9.1).
— Подпись честности про Telegram всегда видна.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 5 — Ре-стайл существующих секций

> Для каждой секции — даю полный итоговый JSX (single source of truth для агента, читающего задачи вне порядка). Тексты берутся из `CONTENT.*` — не дублируются. Логика существующих компонентов (Quiz reducer, Objections accordion) **не меняется** — только классы и обёртки.

### Task 13: Ре-стайл `components/sections/Hero.tsx`

**Файлы:**
- Modify: `components/sections/Hero.tsx`

- [ ] **Step 1: Заменить содержимое файла**

```tsx
import {CONTENT, DIRECT_TG_MESSAGE} from '@/lib/quiz-data';
import {buildTelegramLink} from '@/lib/telegram';
import {Pill} from '@/components/ui/Pill';
import {TrackedLink} from '@/components/ui/TrackedLink';

export function Hero() {
    const tgHref = buildTelegramLink(DIRECT_TG_MESSAGE);
    return (
        <section
            id="hero"
            className="relative isolate mx-auto w-full max-w-4xl overflow-hidden px-4 pb-20 pt-24 md:pb-28 md:pt-32"
        >
            <div className="flex flex-col gap-6">
                <span className="kicker">◆ {CONTENT.hero.kicker}</span>
                <h1 className="font-display text-4xl uppercase leading-[1.04] tracking-tight text-tx md:text-6xl">
                    {CONTENT.hero.h1}
                </h1>
                <p className="max-w-2xl text-base text-tx2 md:text-lg">
                    {CONTENT.hero.sub}
                </p>
                <ul className="flex flex-wrap gap-2">
                    {CONTENT.hero.pills.map((p) => (
                        <li key={p}>
                            <Pill>{p}</Pill>
                        </li>
                    ))}
                </ul>
                <div className="mt-2 flex flex-col gap-3 sm:flex-row sm:items-center">
                    <a href="#test" className="btn btn-primary">
                        {CONTENT.hero.cta}
                    </a>
                    <TrackedLink
                        href={tgHref}
                        goal="lead_click_direct"
                        external
                        className="btn btn-secondary"
                    >
                        ✈ Сразу написать
                    </TrackedLink>
                </div>
                <p className="font-mono text-xs uppercase tracking-[0.14em] text-tx3">
                    {CONTENT.hero.microcopy}
                </p>
            </div>
        </section>
    );
}
```

- [ ] **Step 2: Typecheck**

Run: `npm run typecheck`
Expected: чисто.

- [ ] **Step 3: Commit**

```bash
git add components/sections/Hero.tsx
git commit -m "refactor(sections/Hero): premium .btn-primary/secondary + .kicker

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 14: Ре-стайл `components/sections/Problem.tsx`

**Файлы:**
- Modify: `components/sections/Problem.tsx`

- [ ] **Step 1: Прочитать текущий файл** для понимания структуры (компонент plain Server, без логики).

Run: `cat components/sections/Problem.tsx`

- [ ] **Step 2: Заменить содержимое**

```tsx
import {CONTENT} from '@/lib/quiz-data';

export function Problem() {
    const {kicker, h2, cards, summaryLead, summaryRest} = CONTENT.problem;
    return (
        <section
            id="pain"
            className="mx-auto w-full max-w-4xl px-4 py-20 md:py-28"
        >
            <div className="flex flex-col gap-4">
                <span className="kicker">◆ {kicker}</span>
                <h2 className="font-display text-3xl uppercase tracking-tight text-tx md:text-4xl">
                    {h2}
                </h2>
            </div>

            <div className="mt-10 grid gap-5 md:grid-cols-3">
                {cards.map((c) => (
                    <div key={c.title} className="card-md flex flex-col gap-3">
                        <span className="text-3xl" aria-hidden>
                            {c.emoji}
                        </span>
                        <h3 className="font-display text-lg uppercase tracking-tight text-tx">
                            {c.title}
                        </h3>
                        <p className="text-sm text-tx2">{c.text}</p>
                    </div>
                ))}
            </div>

            <div className="card mt-10">
                <p className="text-base text-tx">
                    <span className="text-cyan">{summaryLead}</span>{' '}
                    <span className="text-tx2">{summaryRest}</span>
                </p>
            </div>
        </section>
    );
}
```

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`
Expected: чисто.

- [ ] **Step 4: Commit**

```bash
git add components/sections/Problem.tsx
git commit -m "refactor(sections/Problem): premium .card-md + .card summary

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 15: Ре-стайл `components/sections/Transformation.tsx`

**Файлы:**
- Modify: `components/sections/Transformation.tsx`

- [ ] **Step 1: Заменить содержимое файла**

```tsx
import {CONTENT} from '@/lib/quiz-data';

export function Transformation() {
    const {kicker, h2, before, bridge, after, afterItems} = CONTENT.transformation;
    return (
        <section
            id="bab"
            className="mx-auto w-full max-w-5xl px-4 py-20 md:py-28"
        >
            <div className="flex flex-col gap-4">
                <span className="kicker">◆ {kicker}</span>
                <h2 className="font-display text-3xl uppercase tracking-tight text-tx md:text-4xl">
                    {h2}
                </h2>
            </div>

            <div className="mt-10 grid gap-6 md:grid-cols-2">
                {/* BEFORE */}
                <div className="card">
                    <p className="font-mono text-xs uppercase tracking-[0.18em] text-tx3">
                        Сейчас
                    </p>
                    <ul className="mt-5 flex flex-col">
                        {before.map((line) => (
                            <li
                                key={line}
                                className="hairline py-3 text-base text-tx2"
                            >
                                {line}
                            </li>
                        ))}
                    </ul>
                </div>

                {/* AFTER */}
                <div
                    className="card"
                    style={{boxShadow: 'var(--edge-highlight), var(--lift), var(--glow-cyan)'}}
                >
                    <p className="font-mono text-xs uppercase tracking-[0.18em] text-cyan">
                        {after}
                    </p>
                    <ul className="mt-5 flex flex-col">
                        {afterItems.map((line) => (
                            <li
                                key={line}
                                className="hairline py-3 text-base text-tx"
                            >
                                {line}
                            </li>
                        ))}
                    </ul>
                </div>
            </div>

            <div className="mt-8 text-center">
                <span className="pill" style={{color: 'var(--color-cyan)'}}>
                    → {bridge} →
                </span>
            </div>
        </section>
    );
}
```

- [ ] **Step 2: Typecheck**

Run: `npm run typecheck`
Expected: чисто.

- [ ] **Step 3: Commit**

```bash
git add components/sections/Transformation.tsx
git commit -m "refactor(sections/Transformation): BAB on .card + hairline + glow-cyan after

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 16: Ре-стайл `components/quiz/*` — Quiz, QuizQuestion, QuizResult, ProgressBar

Тесты в `tests/components/Quiz.test.tsx` проверяют функционал (роли, текст) и должны остаться зелёными.

**Файлы:**
- Modify: `components/quiz/Quiz.tsx`
- Modify: `components/quiz/QuizQuestion.tsx`
- Modify: `components/quiz/QuizResult.tsx`
- Modify: `components/quiz/ProgressBar.tsx`

- [ ] **Step 1: Прочитать существующие файлы**

Run: `cat components/quiz/Quiz.tsx components/quiz/QuizQuestion.tsx components/quiz/QuizResult.tsx components/quiz/ProgressBar.tsx`

Запомни:
- `Quiz` — оркестратор `'use client'` с `useReducer`, рендерит `ProgressBar` + `QuizQuestion` или `QuizResult`. Уже отправляет `quiz_start` / `quiz_complete`.
- `QuizQuestion` — рендерит варианты ответа.
- `QuizResult` — рендерит профиль (badge, title, body, offerTitle, offerText) + кнопку TG + restart.
- `ProgressBar` — простой div.

- [ ] **Step 2: Заменить `components/quiz/Quiz.tsx`**

```tsx
'use client';

import {useEffect, useReducer, useRef} from 'react';
import {QUESTIONS, PROFILES, CONTENT} from '@/lib/quiz-data';
import {quizReducer, initialState} from '@/lib/quiz-logic';
import {reachGoal} from '@/lib/analytics';
import {ProgressBar} from './ProgressBar';
import {QuizQuestion} from './QuizQuestion';
import {QuizResult} from './QuizResult';

const TOTAL = QUESTIONS.length;

export function Quiz() {
    const [state, dispatch] = useReducer(quizReducer, initialState);
    const startSent = useRef(false);
    const completeSent = useRef(false);

    useEffect(() => {
        if (state.finished && state.result && !completeSent.current) {
            completeSent.current = true;
            reachGoal('quiz_complete');
        }
    }, [state.finished, state.result]);

    const handleAnswer = (profile: 'health' | 'body' | 'energy') => {
        if (!startSent.current) {
            startSent.current = true;
            reachGoal('quiz_start');
        }
        dispatch({type: 'ANSWER', profile});
    };

    const handleReset = () => {
        startSent.current = false;
        completeSent.current = false;
        dispatch({type: 'RESET'});
    };

    return (
        <section
            id="test"
            className="mx-auto w-full max-w-3xl px-4 py-20 md:py-28"
        >
            <div className="flex flex-col gap-4">
                <span className="kicker">◆ {CONTENT.quiz.kicker}</span>
                <h2 className="font-display text-3xl uppercase tracking-tight text-tx md:text-4xl">
                    {CONTENT.quiz.h2}
                </h2>
                <p className="text-base text-tx2">{CONTENT.quiz.sub}</p>
            </div>

            <div className="card mt-10">
                <ProgressBar step={state.step} total={TOTAL}/>
                <div className="mt-8">
                    {!state.finished && state.step < TOTAL ? (
                        <QuizQuestion
                            question={QUESTIONS[state.step]}
                            onAnswer={handleAnswer}
                        />
                    ) : state.result ? (
                        <QuizResult profile={PROFILES[state.result]} onRestart={handleReset}/>
                    ) : null}
                </div>
            </div>
        </section>
    );
}
```

- [ ] **Step 3: Заменить `components/quiz/QuizQuestion.tsx`**

```tsx
import type {QuizQuestion as QQ, ProfileKey} from '@/lib/types';

export function QuizQuestion({
    question,
    onAnswer,
}: {
    question: QQ;
    onAnswer: (profile: ProfileKey) => void;
}) {
    return (
        <div className="flex flex-col gap-5">
            <p
                className="font-mono text-xs uppercase tracking-[0.18em] text-tx3"
                aria-label="номер вопроса"
            >
                Вопрос {question.id} / 3
            </p>
            <h3 className="font-display text-xl uppercase tracking-tight text-tx md:text-2xl">
                {question.title}
            </h3>
            <div className="flex flex-col gap-3">
                {question.options.map((opt) => (
                    <button
                        key={opt.label}
                        type="button"
                        onClick={() => onAnswer(opt.scores)}
                        className="card-sm hairline flex flex-col items-start gap-1 text-left transition-all hover:!border-cyan"
                        style={{borderRadius: 'var(--radius-md)'}}
                    >
                        <span className="text-base text-tx">{opt.label}</span>
                        <span className="text-sm text-tx3">{opt.sub}</span>
                    </button>
                ))}
            </div>
        </div>
    );
}
```

- [ ] **Step 4: Заменить `components/quiz/QuizResult.tsx`**

```tsx
import type {Profile} from '@/lib/types';
import {CONTENT} from '@/lib/quiz-data';
import {buildTelegramLink} from '@/lib/telegram';
import {TrackedLink} from '@/components/ui/TrackedLink';

const profileGlow: Record<string, string> = {
    '--color-green': 'var(--glow-green)',
    '--color-pink': 'var(--glow-pink)',
    '--color-orange': 'var(--glow-orange)',
};

export function QuizResult({
    profile,
    onRestart,
}: {
    profile: Profile;
    onRestart: () => void;
}) {
    const tgHref = buildTelegramLink(profile.tgMessage);
    const glow = profileGlow[profile.colorVar] ?? 'var(--glow-cyan)';

    return (
        <div className="flex flex-col gap-5">
            <div className="flex items-center gap-3">
                <span
                    className="inline-block h-3 w-3 rounded-full"
                    style={{
                        backgroundColor: `var(${profile.colorVar})`,
                        boxShadow: glow,
                    }}
                    aria-hidden
                />
                <span
                    className="font-mono text-xs uppercase tracking-[0.18em]"
                    style={{color: `var(${profile.colorVar})`}}
                >
                    {profile.badge}
                </span>
            </div>

            <h3 className="font-display text-2xl uppercase tracking-tight text-tx md:text-3xl">
                {profile.title}
            </h3>
            <p className="text-base text-tx2">{profile.body}</p>

            <div className="mt-2 border-t border-[--line-soft] pt-5">
                <p className="font-mono text-xs uppercase tracking-[0.18em] text-tx3">
                    {profile.offerTitle}
                </p>
                <p className="mt-2 text-base text-tx">{profile.offerText}</p>
            </div>

            <p className="text-xs text-tx3">{CONTENT.quiz.resultNote}</p>

            <div className="mt-2 flex flex-col gap-3 sm:flex-row">
                <TrackedLink
                    href={tgHref}
                    goal={`lead_click_${profile.key}` as const}
                    external
                    className="btn btn-primary"
                >
                    Записаться на бесплатную встречу →
                </TrackedLink>
                <button
                    type="button"
                    onClick={onRestart}
                    className="btn btn-secondary"
                >
                    {CONTENT.quiz.restart}
                </button>
            </div>
        </div>
    );
}
```

- [ ] **Step 5: Заменить `components/quiz/ProgressBar.tsx`**

```tsx
export function ProgressBar({step, total}: { step: number; total: number }) {
    const pct = Math.min(100, Math.round((step / total) * 100));
    return (
        <div className="flex flex-col gap-2" role="progressbar" aria-valuenow={pct} aria-valuemin={0} aria-valuemax={100}>
            <div className="flex items-center justify-between font-mono text-xs uppercase tracking-[0.18em] text-tx3">
                <span>Прогресс</span>
                <span>{pct}%</span>
            </div>
            <div
                className="h-1.5 w-full overflow-hidden rounded-full"
                style={{backgroundColor: 'rgba(255,255,255,0.05)'}}
            >
                <div
                    className="h-full rounded-full transition-all duration-300"
                    style={{
                        width: `${pct}%`,
                        background: 'linear-gradient(90deg, var(--color-cyan), var(--color-violet))',
                        boxShadow: 'var(--glow-cyan)',
                    }}
                />
            </div>
        </div>
    );
}
```

- [ ] **Step 6: Прогнать unit-тесты на Quiz — НЕ должны падать**

Run: `npm test -- tests/components/Quiz.test.tsx`
Expected: PASS (логика не менялась, тесты проверяют функционал).

Если тест на Quiz падает из-за изменённой структуры (например, ожидает определённый текст или роль) — это сигнал: либо изменить тест минимально, чтобы он остался про функционал, либо вернуть в JSX недостающую часть. Не подгонять под неверный путь.

- [ ] **Step 7: Commit**

```bash
git add components/quiz/Quiz.tsx components/quiz/QuizQuestion.tsx components/quiz/QuizResult.tsx components/quiz/ProgressBar.tsx
git commit -m "refactor(quiz): premium .card + .btn + glow per profile color

— Quiz обёртка стала .card.
— QuizQuestion options: .card-sm с hover-glow.
— QuizResult: badge с цветным glow по profile.colorVar, .btn-primary
  для TG, .btn-secondary для restart.
— ProgressBar: градиентная заливка cyan→violet + glow.

Логика reducer / reachGoal не менялась. Quiz.test.tsx остаётся
зелёным.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 17: Ре-стайл `components/sections/Objections.tsx`

Тесты в `tests/components/Objections.test.tsx` проверяют функционал (`aria-expanded`, `objection_open` goal только на первом раскрытии). Они должны остаться зелёными.

**Файлы:**
- Modify: `components/sections/Objections.tsx`

- [ ] **Step 1: Прочитать текущий файл**

Run: `cat components/sections/Objections.tsx`

Запомни логику: `'use client'`, `useState` для открытого индекса, `Set` для уже отправленных goals — чтобы `objection_open` падал только на первом раскрытии.

- [ ] **Step 2: Заменить содержимое (сохранить логику, поменять обёртку и классы)**

```tsx
'use client';

import {useRef, useState} from 'react';
import {CONTENT} from '@/lib/quiz-data';
import {reachGoal} from '@/lib/analytics';

export function Objections() {
    const {kicker, h2, items} = CONTENT.objections;
    const [openIdx, setOpenIdx] = useState<number | null>(null);
    const firedRef = useRef<Set<number>>(new Set());

    const handleToggle = (i: number) => {
        const willOpen = openIdx !== i;
        setOpenIdx(willOpen ? i : null);
        if (willOpen && !firedRef.current.has(i)) {
            firedRef.current.add(i);
            reachGoal('objection_open');
        }
    };

    return (
        <section
            id="objections"
            className="mx-auto w-full max-w-3xl px-4 py-20 md:py-28"
        >
            <div className="flex flex-col gap-4">
                <span className="kicker">◆ {kicker}</span>
                <h2 className="font-display text-3xl uppercase tracking-tight text-tx md:text-4xl">
                    {h2}
                </h2>
            </div>

            <div className="card mt-10">
                {items.map((item, i) => {
                    const open = openIdx === i;
                    return (
                        <div key={item.q} className="hairline">
                            <button
                                type="button"
                                aria-expanded={open}
                                onClick={() => handleToggle(i)}
                                className="flex w-full items-center justify-between gap-4 py-5 text-left transition-colors hover:text-cyan"
                            >
                                <span className="font-display text-base uppercase tracking-tight text-tx md:text-lg">
                                    {item.q}
                                </span>
                                <span
                                    aria-hidden
                                    className="font-mono text-cyan transition-transform"
                                    style={{transform: open ? 'rotate(45deg)' : 'rotate(0deg)'}}
                                >
                                    +
                                </span>
                            </button>
                            {open && (
                                <div className="pb-5 text-base text-tx2">{item.a}</div>
                            )}
                        </div>
                    );
                })}
            </div>
        </section>
    );
}
```

- [ ] **Step 3: Прогнать unit-тесты**

Run: `npm test -- tests/components/Objections.test.tsx`
Expected: 4/4 PASS.

- [ ] **Step 4: Commit**

```bash
git add components/sections/Objections.tsx
git commit -m "refactor(sections/Objections): .card + .hairline accordion, retain logic

aria-expanded, single-open behavior и objection_open-only-on-first
поведение сохранены — тесты §15.5 остаются зелёными.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 18: Ре-стайл `components/sections/FinalCta.tsx`

**Файлы:**
- Modify: `components/sections/FinalCta.tsx`

- [ ] **Step 1: Прочитать текущий файл**

Run: `cat components/sections/FinalCta.tsx`

- [ ] **Step 2: Заменить содержимое**

```tsx
import {CONTENT, DIRECT_TG_MESSAGE} from '@/lib/quiz-data';
import {buildTelegramLink} from '@/lib/telegram';
import {TrackedLink} from '@/components/ui/TrackedLink';

export function FinalCta() {
    const {kicker, h2, text, cta1, cta2, guarantee} = CONTENT.finalCta;
    const tgHref = buildTelegramLink(DIRECT_TG_MESSAGE);
    return (
        <section
            id="final"
            className="mx-auto w-full max-w-3xl px-4 py-20 md:py-28"
        >
            <div className="card text-center">
                <span className="kicker">◆ {kicker}</span>
                <h2 className="mt-4 font-display text-3xl uppercase tracking-tight text-tx md:text-4xl">
                    {h2}
                </h2>
                <p className="mx-auto mt-4 max-w-xl text-base text-tx2">{text}</p>
                <div className="mt-8 flex flex-col gap-3 sm:flex-row sm:justify-center">
                    <a href="#test" className="btn btn-primary">
                        {cta1}
                    </a>
                    <TrackedLink
                        href={tgHref}
                        goal="lead_click_direct"
                        external
                        className="btn btn-secondary"
                    >
                        {cta2}
                    </TrackedLink>
                </div>
                <p className="mt-5 font-mono text-xs uppercase tracking-[0.14em] text-tx3">
                    {guarantee}
                </p>
            </div>
        </section>
    );
}
```

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`
Expected: чисто.

- [ ] **Step 4: Commit**

```bash
git add components/sections/FinalCta.tsx
git commit -m "refactor(sections/FinalCta): premium .card center + dual btn

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 6 — Сборка

### Task 19: Финальная `app/page.tsx` — порядок секций + bridges + ре-стайл footer

**Файлы:**
- Modify: `app/page.tsx`

- [ ] **Step 1: Заменить содержимое файла**

```tsx
import {Hero} from '@/components/sections/Hero';
import {Problem} from '@/components/sections/Problem';
import {LocationMap} from '@/components/sections/LocationMap';
import {Transformation} from '@/components/sections/Transformation';
import {Quiz} from '@/components/quiz/Quiz';
import {BookingSlot} from '@/components/sections/BookingSlot';
import {Objections} from '@/components/sections/Objections';
import {FinalCta} from '@/components/sections/FinalCta';
import {StickyCta} from '@/components/ui/StickyCta';
import {Bridge} from '@/components/ui/Bridge';
import {CONTENT, BRIDGES} from '@/lib/quiz-data';

export default function Home() {
    return (
        <>
            <Hero/>
            <Bridge data={BRIDGES.toProblem}/>
            <Problem/>
            <Bridge data={BRIDGES.toMap}/>
            <LocationMap/>
            <Bridge data={BRIDGES.toTransformation}/>
            <Transformation/>
            <Bridge data={BRIDGES.toQuiz}/>
            <Quiz/>
            <Bridge data={BRIDGES.toBooking}/>
            <BookingSlot/>
            <Bridge data={BRIDGES.toObjections}/>
            <Objections/>
            <FinalCta/>
            <footer className="mx-auto w-full max-w-4xl px-4 py-12 text-center">
                <div className="border-t border-[--line-soft] pt-10">
                    <p className="font-display text-base uppercase tracking-tight text-tx">
                        {CONTENT.footer.name}
                    </p>
                    <p className="mt-2 font-mono text-xs uppercase tracking-[0.14em] text-tx3">
                        {CONTENT.footer.tagline}
                    </p>
                    <p className="mt-5 text-xs text-tx3">
                        <a href="/privacy" className="underline hover:text-tx2">
                            Политика конфиденциальности
                        </a>
                    </p>
                </div>
            </footer>
            <StickyCta/>
        </>
    );
}
```

- [ ] **Step 2: Typecheck + build**

Run: `npm run typecheck && npm run build`
Expected: оба чисто.

- [ ] **Step 3: Прогнать dev и пройтись по странице вручную**

Run: `npm run dev`
Открой `http://localhost:3000`. Проверь визуально:
- Все секции рендерятся (Hero → Problem → LocationMap → Transformation → Quiz → BookingSlot → Objections → FinalCta).
- Между ними — bridges (короткие вопросы + пилюли-якоря).
- Sticky CTA появляется на мобиле после скролла.
- Footer в подвале с разделителем сверху.

Останови dev (`Ctrl+C`).

- [ ] **Step 4: Commit**

```bash
git add app/page.tsx
git commit -m "feat(page): new section order + bridges between sections + premium footer

SPEC §6. Порядок: Hero → Problem → LocationMap → Transformation →
Quiz → BookingSlot → Objections → FinalCta. Bridges (BRIDGES.toX) —
интерактивные мостики между каждой парой секций. Footer теперь
открытый блок на градиенте body с .hairline сверху.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 7 — E2E тесты

### Task 20: Новый `tests/e2e/booking-slot.spec.ts`

**Файлы:**
- Create: `tests/e2e/booking-slot.spec.ts`

- [ ] **Step 1: Прочитать существующий e2e для понимания паттерна**

Run: `cat tests/e2e/quiz-flow.spec.ts`

- [ ] **Step 2: Создать новый файл**

```ts
import {test, expect} from '@playwright/test';

test.describe('BookingSlot e2e (SPEC §9, §15)', () => {
    test('выбор чипа → href ссылки содержит время; клик ведёт на t.me/...', async ({page}) => {
        await page.goto('/');
        await page.locator('#booking').scrollIntoViewIfNeeded();

        // CTA disabled до выбора
        const cta = page.getByTestId('booking-cta');
        await expect(cta).toHaveAttribute('aria-disabled', 'true');

        // Выбираем первый чип
        const firstChip = page.locator('#booking button[aria-pressed]').first();
        const chipText = (await firstChip.innerText()).toLowerCase();
        await firstChip.click();
        await expect(firstChip).toHaveAttribute('aria-pressed', 'true');

        // CTA стал ссылкой <a> на t.me/
        await expect(cta).toHaveAttribute('href', /^https:\/\/t\.me\//);
        const href = await cta.getAttribute('href');
        expect(href).toBeTruthy();

        // В URL должен быть encoded день недели или время из чипа
        const decoded = decodeURIComponent(href!);
        const timeMatch = chipText.match(/\d{2}:\d{2}/);
        expect(timeMatch).not.toBeNull();
        expect(decoded).toContain(timeMatch![0]);
    });

    test('подпись честности видна; нет «забронировано»/«занято» в DOM', async ({page}) => {
        await page.goto('/');
        await expect(page.getByText(/откроется telegram/i)).toBeVisible();

        // Кликаем чип + CTA — никаких слов про бронь не должно появиться
        const firstChip = page.locator('#booking button[aria-pressed]').first();
        await firstChip.click();
        await page.getByTestId('booking-cta').click({modifiers: ['Meta'], force: true}).catch(() => {
            // На некоторых платформах ссылка открывается в новой вкладке —
            // нам важно только что DOM на основной странице не показал «забронировано».
        });

        const body = await page.locator('body').innerText();
        const lower = body.toLowerCase();
        for (const word of ['забронирован', 'занято', 'зарезервирован', 'ваш слот', 'место за вами']) {
            expect(lower).not.toContain(word);
        }
    });
});
```

- [ ] **Step 3: Запустить новый e2e**

Run: `npm run test:e2e -- booking-slot`
Expected: 2/2 PASS.

- [ ] **Step 4: Commit**

```bash
git add tests/e2e/booking-slot.spec.ts
git commit -m "test(e2e): BookingSlot — chip → TG href; no 'забронировано' in DOM

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 21: Расширить `tests/e2e/quiz-flow.spec.ts` — переход к #booking

**Файлы:**
- Modify: `tests/e2e/quiz-flow.spec.ts`

- [ ] **Step 1: Прочитать текущий файл**

Run: `cat tests/e2e/quiz-flow.spec.ts`

- [ ] **Step 2: Дописать в конец `describe`-блока новый тест**

В конец существующего `test.describe` блока вставь:

```ts
    test('bridge от Quiz к BookingSlot — клик ведёт к #booking', async ({page}) => {
        await page.goto('/#test');

        // Скроллим в зону bridge toBooking (он между Quiz и BookingSlot)
        const bridgeLink = page.locator('a[href="#booking"]').first();
        await bridgeLink.scrollIntoViewIfNeeded();
        await expect(bridgeLink).toBeVisible();

        await bridgeLink.click();

        // Проверяем что секция #booking видна в viewport
        const booking = page.locator('#booking');
        await expect(booking).toBeInViewport();
    });
```

- [ ] **Step 3: Запустить**

Run: `npm run test:e2e -- quiz-flow`
Expected: все тесты в файле PASS, включая новый.

- [ ] **Step 4: Commit**

```bash
git add tests/e2e/quiz-flow.spec.ts
git commit -m "test(e2e): bridge Quiz → #booking section

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 22: Расширить `tests/e2e/reduced-motion.spec.ts` — радар не анимируется

**Файлы:**
- Modify: `tests/e2e/reduced-motion.spec.ts`

- [ ] **Step 1: Прочитать текущий файл**

Run: `cat tests/e2e/reduced-motion.spec.ts`

- [ ] **Step 2: Дописать в конец `describe`-блока новый тест**

В конец существующего `test.describe` блока вставь:

```ts
    test('радар в LocationMap имеет CSS animation: none при reduced-motion', async ({browser}) => {
        const context = await browser.newContext({reducedMotion: 'reduce'});
        const page = await context.newPage();
        await page.goto('/');

        const ring = page.locator('#map .radar-ring').first();
        await ring.scrollIntoViewIfNeeded();

        const animation = await ring.evaluate((el) => getComputedStyle(el).animationName);
        // При reduced-motion CSS-правило перекрывает animation на none
        expect(animation === 'none' || animation === '').toBeTruthy();

        await context.close();
    });
```

- [ ] **Step 3: Запустить**

Run: `npm run test:e2e -- reduced-motion`
Expected: PASS, включая новый.

- [ ] **Step 4: Commit**

```bash
git add tests/e2e/reduced-motion.spec.ts
git commit -m "test(e2e): radar animation disabled under prefers-reduced-motion

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Phase 8 — Финальная проверка

### Task 23: Полный прогон всех проверок

**Файлы:** ничего не меняется (только команды).

- [ ] **Step 1: Typecheck**

Run: `npm run typecheck`
Expected: 0 ошибок.

- [ ] **Step 2: Lint**

Run: `npm run lint`
Expected: чисто (warnings — обсуждаются, errors — нет).

- [ ] **Step 3: Unit-тесты**

Run: `npm test`
Expected: ВСЕ зелёные. Проверь, что в выводе есть:
- `quiz-logic.test.ts` — PASS
- `telegram.test.ts` — PASS
- `analytics.test.ts` — PASS
- `Quiz.test.tsx` — PASS
- `Objections.test.tsx` — PASS
- `BookingSlot.test.tsx` — PASS (новые)
- `slots-data.test.ts` — PASS (новые)

- [ ] **Step 4: E2E тесты**

Run: `npm run test:e2e`
Expected: ВСЕ зелёные. Проверь:
- `mobile-sticky.spec.ts` — PASS
- `quiz-flow.spec.ts` — PASS (включая новый bridge-тест)
- `reduced-motion.spec.ts` — PASS (включая новый radar-тест)
- `booking-slot.spec.ts` — PASS (новый файл)

- [ ] **Step 5: Production build**

Run: `npm run build`
Expected: успешная сборка. Без unused imports, без TS-ошибок.

- [ ] **Step 6: Если что-то упало — НЕ маскировать**

Если упал тест после рестайла — это сигнал, что класс/aria-attribute, на который полагался тест, реально потерян. Восстанови нужный атрибут в JSX (например, `aria-expanded`, `data-testid`, `role`), а НЕ ослабляй тест. Если упал typecheck — посмотри сначала на `lib/types.ts` и `lib/quiz-data.ts` (источники правды).

- [ ] **Step 7: Если всё чисто — commit chore-маркера**

```bash
git commit --allow-empty -m "chore: sprint-2 — all checks green (tsc/lint/vitest/playwright/build)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 24: Visual Gate — ручная сверка с `VISUAL-REFERENCE.html`

**Файлы:** ничего. Это ручной чек.

- [ ] **Step 1: Запустить dev**

Run: `npm run dev`
Открой две вкладки:
- `http://localhost:3000`
- `file:///<абсолютный_путь>/VISUAL-REFERENCE.html`

- [ ] **Step 2: Пройти чек-лист по каждой секции**

Секции: Hero · Problem · LocationMap · Transformation · Quiz · BookingSlot · Objections · FinalCta · Footer.

Для каждой секции:
- [ ] Градиентный фон body виден (не плоский `#0E1117`).
- [ ] У карточек видна многослойная тень (`--lift`).
- [ ] У карточек есть верхняя кромка-highlight (тонкая светлая полоса наверху).
- [ ] Радиус карточек 30-42px (не 8-12px).
- [ ] Hairline-разделители 1px внутри карточек (где применимо: Transformation BAB-списки, Objections, FinalCta — обёртка одна).
- [ ] Glow на акцентах **светится** (box-shadow с alpha), не плоский кружок (точки профилей, главная кнопка, активный чип).
- [ ] Воздух между секциями ~80-100px (не 16-32px).
- [ ] Заголовок — Gerhaus (или Georgia fallback), uppercase, letter-spacing отрицательный.
- [ ] Kicker — JetBrains Mono, uppercase, трекинг ≥+2.4px.

Если хоть один пункт «нет» — отметить в комментарии PR/отчёта и вернуться к соответствующей задаче ре-стайла. Не двигаться дальше пока не «по породе».

- [ ] **Step 3: Прогон мобильной адаптации**

В DevTools открой mobile view (iPhone 12 Pro, например). Проверь:
- LocationMap — 3 колонки превратились в стек.
- BookingSlot — чипы переразложились через `auto-fit minmax(170px, 1fr)`.
- Sticky-CTA появляется снизу после скролла.
- Все touch-цели (кнопки, чипы) визуально ≥44×44px.

- [ ] **Step 4: Прогон reduced-motion**

В DevTools → Rendering → Emulate CSS media feature `prefers-reduced-motion: reduce`. Открой LocationMap. Радар не двигается (статичные кольца с opacity 0.4).

- [ ] **Step 5: Запиши отчёт прохождения visual gate**

Не обязательный пункт, но рекомендую — создать короткий лог (например, в PR-описании):
```
Visual gate sprint 2 — passed
- Hero: ✓
- Problem: ✓
- LocationMap: ✓
- Transformation: ✓
- Quiz: ✓
- BookingSlot: ✓
- Objections: ✓
- FinalCta: ✓
- Footer: ✓
Mobile: ✓
Reduced motion: ✓
```

- [ ] **Step 6: Финальный commit (маркер)**

```bash
git commit --allow-empty -m "chore: sprint-2 visual gate passed (manual review)

Все 8 секций + footer проверены на 5 приёмов премиальности §5
рядом с VISUAL-REFERENCE.html. Mobile-layout ок. Reduced-motion
глушит радар.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Готово

После всех 24 задач:
- Premium-уровень визуала (5 приёмов §5 спеки на каждой секции).
- Новые секции `LocationMap` и `BookingSlot` в проде, на премиум-классах.
- Аналитика расширена `slot_select` / `slot_take`.
- Тесты Vitest + Playwright покрывают новый функционал; старые тесты остались зелёными.
- Контент-плейсхолдеры с TODO помечены (слоты + ЖК) — заменяются перед деплоем заказчиком.

**Что НЕ сделано в спринте 2 (по дизайну, явно отложено в фазу 2):**
- Реальная бронь через бэк.
- Серверный приём заявок.
- Реальные минуты ЖК (требует Яндекс.Карт).
- Финальные слоты от тренера (плейсхолдеры до согласования).
